package main

// TODOS

// func - use QR resolve command from cards/cards.go to get links and update fragrances DB (report mismatch on existing ones)

// func - get details of newly found fragrance, parse html? - ugh!, alternative: use ML and get it from the cards...

// func - periodically (once an hour or so, after figuring out hosting) check every existing frag and update score (anything else?) if changed
